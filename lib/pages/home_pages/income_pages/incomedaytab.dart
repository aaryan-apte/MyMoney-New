import 'package:flutter/material.dart';

class IncomeDayTab extends StatefulWidget {
  const IncomeDayTab({Key? key}) : super(key: key);

  @override
  State<IncomeDayTab> createState() => _IncomeDayTabState();
}

class _IncomeDayTabState extends State<IncomeDayTab> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
